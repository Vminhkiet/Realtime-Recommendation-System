# --- 1. Simulation & Ingestion (Cho Producer) ---
kafka-python==2.0.2       # Thư viện kết nối Kafka
Faker==24.0.0             # Thư viện tạo dữ liệu giả (IP, Tên, Địa chỉ)

# --- 2. Data Processing & Big Data (Cho Spark Dev) ---
# Cài PySpark để code hỗ trợ nhắc lệnh (Intellisense) trên VS Code
pyspark==3.5.1
pandas>=2.2.3
numpy==1.26.4
pyarrow==15.0.2           # Hỗ trợ đọc/ghi file Parquet nhanh
redis==5.0.1

# --- 3. AI Core (Cho SASRec Model) ---
# Hệ sinh thái Keras 3 & TensorFlow mới nhất
tensorflow==2.19.0       # Backend tính toán
scikit-learn==1.4.1.post1 # Dùng để chia tập train/test hoặc tính metrics
keras-rs==0.2.1
keras-hub==0.21.1

pymongo==4.7.1


# --- 4. Dashboard (Cho Serving Layer) ---
streamlit==1.32.2         # Framework làm Web App cực nhanh
plotly==5.20.0            # Vẽ biểu đồ tương tác đẹp hơn mặc định
altair==5.2.0             # Thư viện vẽ biểu đồ chuẩn của Streamlit

# --- 5. Utilities (Tiện ích) ---
python-dotenv==1.0.1      # Đọc file .env
requests==2.31.0          # Gọi API (nếu cần)
tqdm==4.66.2              # Hiện thanh loading bar khi chạy vòng lặp

# --- 6. Database Drivers (BẮT BUỘC CHO DASHBOARD) ---
# Để Dashboard kết nối được với TimescaleDB (PostgreSQL)
psycopg2-binary==2.9.9
SQLAlchemy==2.0.28        # Hỗ trợ pandas đọc SQL xịn hơn

# --- 7. Data Analysis & Visualization (Cho inspect_data.py) ---
# Để chạy file kiểm tra dữ liệu và vẽ biểu đồ phân bố rating
matplotlib==3.8.3
seaborn==0.13.2

# --- 8. Avro & Schema Registry (Cho Nâng cao) ---
# Nếu bạn muốn chuyển sang dùng Avro như đã hỏi
confluent-kafka==2.3.0    # Client Kafka xịn hơn kafka-python (hỗ trợ Avro tốt)
fastavro==1.9.4           # Xử lý file Avro siêu tốc
avro==1.11.3              # Thư viện chuẩn
boto3==1.34.0